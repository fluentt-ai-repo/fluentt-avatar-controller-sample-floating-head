# v0.3.1 - SuppressBaseExpression 호출 제거

**릴리스 날짜**: 2026-04-03

## 주요 변경사항

### Base Expression 항상 활성화

Gesture, OneShot Motion 재생 시 `SuppressBaseExpression`을 호출하여 Base Expression을 비활성화하던 코드를 제거했습니다.

- 이전: Gesture/OneShot Motion 재생 시 `SuppressBaseExpression(true)` → 종료 시 `SuppressBaseExpression(false)` 호출
- 이후: Base Expression이 항상 활성 상태로 유지됨

## 변경 파일

### Runtime
- `FluentTAvatarControllerFloatingHead.OneShotMotion.cs` — SuppressBaseExpression 호출 제거
- `FluentTAvatarControllerFloatingHead.ServerMotionTagging.cs` — SuppressBaseExpression 호출 제거
